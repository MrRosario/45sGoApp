import React, { FC } from "react";
import { View, StyleSheet, Text } from "react-native";

const Exercise: FC = () => {
    return(
        <View>
           <Text>Exerciosss</Text>
        </View>
    )
}

const styles = StyleSheet.create({
   
});

export default Exercise;